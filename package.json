{
  "name": "umineco",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "engines": {
    "node": "4"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build"
  }
}
